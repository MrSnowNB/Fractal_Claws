ticket_id: TASK-003
version: '1.0'
status: open
created_at: '2026-04-07'
updated_at: '2026-04-07'
title: "Verify fib.py output contains 55"

# PHASE 1 PROOF GATE — upstream context injection test
# This ticket depends on TASK-002.
# Purpose: validate that:
#   1. drain() correctly blocks TASK-003 until TASK-002 closes
#   2. inject_upstream_context() pulls TASK-002 result into this prompt
#   3. The model can use upstream context to confirm a condition
#
# Pass criteria:
#   - TASK-002 must be closed before this ticket runs (deps_met check)
#   - This ticket reads output/fib.py and confirms 55 is present
#   - Closes with status: closed on first attempt

task: |
  Read the file output/fib.py and execute it. Confirm that the output
  contains the number 55. If it does, write a one-line confirmation to
  output/fib_verify.txt containing exactly: VERIFIED: 55 found in fib output
  What done looks like: output/fib_verify.txt exists and contains VERIFIED.

context_files: []
depends_on:
  - TASK-002
allowed_tools:
  - read_file
  - exec_python
  - write_file

depth: 0
decrement: 3
max_tokens: null
result_path: logs/TASK-003-result.txt
attempts_log: logs/TASK-003-attempts.jsonl
timeout_seconds: 120
