ticket_id: TASK-002
version: '1.0'
status: open
created_at: '2026-04-07'
updated_at: '2026-04-07'
title: "Fibonacci script: write, execute, verify"

# PHASE 1 PROOF GATE — depends_on chain test
# This ticket deliberately depends on TASK-003 (the verify step).
# Purpose: validate the full dependency chain:
#   decompose_budget -> ticket ordering -> depends_on resolution ->
#   upstream context injection -> exec_python sandbox
#
# Pass criteria:
#   - output/fib.py exists and executes without error
#   - stdout contains 55 (the 10th Fibonacci number)
#   - TASK-003 closes with upstream context from this ticket

task: |
  Write a Python script to output/fib.py that prints the first 10 Fibonacci
  numbers, one per line, starting from 1. Then execute it.
  What done looks like: The script runs, returncode is 0,
  and stdout contains the number 55 on its own line.

context_files: []
depends_on: []
allowed_tools:
  - write_file
  - exec_python

depth: 0
decrement: 3
max_tokens: null
result_path: logs/TASK-002-result.txt
attempts_log: logs/TASK-002-attempts.jsonl
timeout_seconds: 120
